# Configuration de l'application
# Configuration de l'application
spring.application.name=Authentification-module
server.port=8086

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.registry-fetch-interval-seconds=5
eureka.client.initial-instance-info-replication-interval-seconds=5
eureka.client.eureka-server-connect-timeout-seconds=5
eureka.client.eureka-server-read-timeout-seconds=8
eureka.client.eureka-service-url-poll-interval-seconds=300
eureka.client.max-retry-attempts=10
eureka.instance.instance-id=${spring.application.name}:${random.uuid}
eureka.instance.lease-renewal-interval-in-seconds=10
eureka.instance.lease-expiration-duration-in-seconds=30
# Configuration DB
spring.datasource.url=jdbc:postgresql://localhost:5433/looyas-pfe
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.tomcat.test-on-borrow=true
spring.datasource.tomcat.validation-query=SELECT 1


# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true


# Email (conservez votre configuration existante)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username= khalilsaidani0077@gmail.com
spring.mail.password= kyuxbfhhfnqtuvpk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT (conservez votre configuration existante)
jwt.expiration=19000000
jwt.secret=your-256-bit-secret-key-must-be-at-least-32-bytes-long
# etape

# URL du REST API Camunda
camunda.rest-url=http://localhost:8090/engine-rest

# Cl� de votre process BPMN
camunda.process.key=Workflow_Conge

# D�finitions des tasks (identifiants dans le BPMN)
camunda.task.manager=UserTask_ValidationManager
camunda.task.rh=UserTask_ValidationRH

# Groupes candidats
camunda.group.manager=managers
camunda.group.rh=rh

# Topics External Task
camunda.topic.rejection=send-rejection-email
camunda.topic.final-approval=send-final-approval-email
# --- Camunda REST & process key (d�j� en place) ---


# --- Liste dynamique des �tapes de validation ---
# S�quence de vos userTask Ids tels qu?ils apparaissent dans le BPMN
camunda.task.keys=UserTask_ValidationManager,UserTask_ValidationRH

# Cl�s de variables � remplir pour chaque t�che
camunda.task.decision-keys=decisionManager,decisionRH
camunda.task.comment-keys=commentaireManager,commentaireRH

# Statuts � appliquer quand on approuve / rejette (m�mes longueurs que ci-dessus)
camunda.task.approve-statuses=Validated by Manager,Validated by HR
camunda.task.reject-statuses=Rejected by Manager,Rejected by HR
# Quand la t�che Manager est approuv�e, quel status mettre ?
# Puis le m�me pour RH.
# calcule de salaire

salaire.cnss.rate=0.0918
salaire.deduction.forfaitaire.max=2000.0
salaire.deduction.forfaitaire.percent=0.10

# RabbitMq.
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest
nlp.api.url=http://localhost:8000

jwt.cleanup.fixed-rate-ms=60000

spring.mail.protocol=smtp
spring.mail.test-connection=false